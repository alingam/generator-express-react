language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-express-react' ]; then cd .. && eval "mv $currentfolder generator-express-react" && cd generator-express-react; fi

